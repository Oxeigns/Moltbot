{
  "name": "moltbot-heroku-wrapper",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "postinstall": "npm install -g pnpm && pnpm install && pnpm build",
    "start": "bash start.sh"
  },
  "dependencies": {
    "moltbot": "github:moltbot/clawdbot"
  }
}
