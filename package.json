{
  "name": "moltbot-heroku-wrapper",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "heroku-postbuild": "npm i -g pnpm@latest && git clone --depth 1 https://github.com/moltbot/moltbot.git moltbot-src && cd moltbot-src && pnpm install && pnpm build && ls -la dist",
    "start": "bash start.sh"
  }
}
