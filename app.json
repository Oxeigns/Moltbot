{
  "name": "moltbot-heroku-wrapper",
  "description": "Heroku worker-only wrapper for Moltbot using global CLI install.",
  "keywords": [
    "moltbot",
    "telegram",
    "heroku",
    "worker"
  ],
  "env": {
    "TELEGRAM_BOT_TOKEN": {
      "description": "Telegram bot token from BotFather.",
      "required": true
    },
    "OPENAI_API_KEY": {
      "description": "OpenAI API key used by the OpenAI provider.",
      "required": true
    },
    "MODEL": {
      "description": "OpenAI model to use.",
      "required": false,
      "value": "gpt-4o-mini"
    },
    "GROUP_REQUIRE_MENTION": {
      "description": "Require @mention in groups before Moltbot responds.",
      "required": false,
      "value": "true"
    },
    "LOG_LEVEL": {
      "description": "Logging level passed to Moltbot (info, debug, warn, error).",
      "required": false,
      "value": "info"
    }
  }
}
